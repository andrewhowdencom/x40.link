---
version: "2"

linters:
  # Keep the baseline stable when golangci-lint changes its defaults.
  default: standard
  enable:
    - revive
  settings:
    revive:
      rules:
        # Exported constructors intentionally return package-private types.
        - name: unexported-return
          disabled: true
